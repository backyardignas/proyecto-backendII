spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mi_base_de_datos?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: Jose_Java
    password: Jose_JMN0306
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true